[package]
name = "build-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
rom-manager = { path = "../../libs/rom-manager" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
