# Session — 2026-06-16

## Goal
Replace the collapsible text list of missing games in Build UI with a proper MUI `<Table>` showing game name, status (Missing/CRC Error), details, and a "View" button.

## Files Changed
- `apps/rom-manager-ui/src/components/BuildManager.jsx`
  - Added `useNavigate` import for game detail navigation
  - Added MUI `<Table>` with columns: Game, Status, Details, Action
  - Row click and "View" button navigate to `/collections/{id}?q={gameName}`
  - Removed unused `Collapse` import and `showMissing` state

## Commands Run
- `npx vite build` — build succeeded

## Branch
- `agent/build-result-table`

## Commit(s)
- `ce82a4f` feat: replace missing games collapsible list with MUI table in build results
