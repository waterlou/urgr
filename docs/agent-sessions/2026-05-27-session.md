# Session Summary — 2026-05-27

## Goal
Refactor server codebase and add mobile-friendly UI features.

## Files Changed
- `AGENTS.md` — created with project conventions, build commands, workflow rules
- `apps/rom-manager-ui/server/helpers.js` — created: shared DB helpers (all, get, run/runNow, dbReady, dbPath)
- `apps/rom-manager-ui/server/routes/*.js` — 7 route files extracted from monolithic index.js:
  - collections.js (collection CRUD, builds, scan/verify, exports, IA download, status, platforms)
  - games.js (browse, detail, scrape, batch-scrape, scrape-jobs, rating, cover)
  - game-sets.js (game set CRUD, game membership, exports)
  - versions.js (version listing, game lookup, import-online, import-dat, fill-descriptions)
  - scraper.js (search, scrape, hash, detail proxy)
  - ia.js (Internet Archive file listing and download)
  - misc.js (SSE job stream, cancel, settings CRUD, test-tgdb/igdb)
- `apps/rom-manager-ui/server/index.js` — rewritten as thin boot file (80 lines), mounts routers, global error handler
- `apps/rom-manager-ui/src/App.css` — view-stack slide animation, detail-page layout, hamburger + mobile sidebar overlay
- `apps/rom-manager-ui/src/App.jsx` — view-stack detail navigation, sidebarOpen state, mobile sidebar toggle
- `apps/rom-manager-ui/src/components/GameDetail.jsx` — converted from modal popup to full-width detail page with back navigation, loading skeleton, lightbox via createPortal
- `apps/rom-manager-ui/src/components/GameBrowser.jsx` — added hamburger button, `onToggleSidebar` prop
- `apps/rom-manager-ui/src/components/Sidebar.jsx` — added `sidebarOpen` prop for mobile overlay class
- `apps/rom-manager-ui/src/components/CollectionForm.jsx` — fixed edit mode dataset validation bug; skip dataset checks when editing

## Key Fixes
- Collection edit: "Select a preset dataset" error when editing a collection with has_dataset=1
- Collection edit: slug not persisted on PUT (silently dropped in handler)
- Collection edit: folder rename on disk when slug changes
- Sidebar backdrop with `pointer-events: none` when hidden (was capturing hamburger clicks)
- Lightbox portaled to `document.body` to escape view-stack stacking context

## Features Added
- View-stack slide-in/out game detail navigation (iOS-style push/pop)
- Hamburger menu + sidebar slide-out drawer on mobile (≤768px)
- Sidebar auto-closes on navigation, backdrop fade to close

## Commands Run
- `npm run test:api` — 14/14 passed
- `npm run test:ui` — 7/7 passed
- `npx vite build` — builds successfully
