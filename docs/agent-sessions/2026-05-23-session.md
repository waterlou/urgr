# Session: 2026-05-23 (afternoon)

## Goal
Full project audit: find orphaned files, broken references, misconfigurations.

## Issues Found & Fixed
1. Missing `.gitignore` — created root + UI-level
2. `~/` directory — 500MB Chrome debug profile deleted
3. Orphaned components — `HeroBanner.jsx`, `NavBar.jsx`, `GameRow.jsx`, `GameCard.jsx` (unused, deleted)
4. Stale `roms.db` in UI app dir — deleted
5. `.dockerignore` — added `node_modules/`, `roms.db*`, `roms/`, `~/`
6. `quick-xml` version mismatch in `rom-scraper` Cargo.toml (0.31 vs 0.36) — bumped
7. SPECS.md stale references — updated

## Files changed
- `.gitignore` (created)
- `apps/rom-manager-ui/.gitignore` (created)
- `.dockerignore` (updated)
- `Dockerfile` (merged CLI + UI builds)
- `docker-compose.yml` (single container, mounts roms/config)
- `libs/rom-scraper/Cargo.toml` (quick-xml bump)
- `apps/rom-manager-ui/docs/SPECS.md` (updated)
- `apps/rom-manager-ui/server/cli.js` (new — exec wrapper for Rust CLI)
- `apps/rom-manager-ui/server/db.js` (added getDbPath)
- `apps/rom-manager-ui/server/index.js` (added /api/cli/* routes)
- `apps/rom-manager-ui/src/api.js` (added cliScan/cliVerify/cliHash)
- `apps/rom-manager-ui/src/components/CollectionDetail.jsx` (added Scan & Verify UI)

## Uncommitted items for next session
- `dataset_preset` field sent in POST but never stored in INSERT query
- `res.sendFile()` at line 859 lacks error handling
- Direct state mutation in GameBrowser.jsx (React anti-pattern)
