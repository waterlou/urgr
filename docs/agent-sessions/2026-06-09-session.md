# Session: 2026-06-09 EmulatorJS fixes + version sort + path fixes

## Goal
Fix EmulatorJS audio leak on close, version ordering, `.version` file path bugs, and reimport FBNeo v1.0.0.02 to fix empty platform.

## Files Changed
- `apps/rom-manager-ui/src/components/EmulatorModal.jsx` — close Web Audio context on modal destroy; use nightly CDN
- `apps/rom-manager-ui/server/routes/collections.js` — sort versions by `sortVersions()` instead of `created_at DESC`
- `apps/rom-manager-ui/server/routes/versions.js` — fix `.version` file path (slug→folder, 3→4 levels up)
- `apps/rom-manager-ui/server/routes/games.js` — fix `path.resolve` depth (3→4 levels up) in 3 places
- `apps/rom-manager-ui/server/index.js` — add `uncaughtException` handler
- `apps/rom-manager-ui/server/versionSort.js` — new shared sort function
- `apps/rom-manager-ui/server/test-version-sort.mjs` — 14 unit tests

## Commands Run
- `bash server/test-api.sh` — 14/14 passed
- `npm run test:nps` — 21/21 passed
- `node --test server/test-version-sort.mjs` — 14/14 passed
- `npx vite build` — rebuilt frontend
- Reimported FBNeo v1.0.0.02 via API
- DB cleanup: normalized NULL region, fixed empty platform, deduped entries

## Key Fixes
1. **EmulatorJS audio leak**: `audioCtx.close()` + `Module.pauseMainLoop()` on modal unmount
2. **EmulatorJS nightly CDN**: stable CDN's fbneo core has stale ROM database
3. **Version ordering**: `getCollectionVersions` now sorts oldest first, nightly last
4. **`.version` file path**: was `col.slug` (testfbneo), should be `col.folder` (fbneo)
5. **path.resolve depth**: routes/ files need 4 levels up, not 3
6. **Empty platform**: reimported v1.0.0.02, cleaned up NULL region duplicates
