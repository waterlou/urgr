# Session — 2026-06-17

## Goal
1. Show missing games table for both build and scan results
2. Remove the Version/Format/Status/Progress/Date/Actions builds history table
3. Fix FileNotFound detection (all missing games showed CRC Error)
4. Row click opens popup showing per-ROM details (filename, expected CRC, actual CRC, status)

## Files Changed

### Rust
- `libs/rom-manager/src/models.rs` — Added `RomDetail` struct, added `rom_details` field to `MissingGame`
- `libs/rom-manager/src/lib.rs` — Added `RomDetail` to re-exports
- `libs/rom-manager/src/builder.rs` — Added `read_zip_crc_by_filename` function; updated `ImportIndex::explain_missing` to return `(MissingReason, Vec<RomDetail>)`; updated call site to pass `rom_details`

### Server
- `apps/rom-manager-ui/server/routes/collections.js` — Scan result now includes `missing_names` and `matched_names` arrays for frontend display

### Frontend
- `apps/rom-manager-ui/src/components/BuildManager.jsx` — Complete rewrite:
  - Fixed FileNotFound detection: `typeof r.reason === 'string'` (serde serializes unit variants as strings, not objects)
  - Removed builds history table (Version Format Status Progress Date Actions)
  - Shows missing games table for both build results (`MissingGamesTable` with per-ROM detail) and scan results (`MissingScanTable` with name-only list)
  - Build results: "Details" button / row click opens `RomDetailDialog` modal with ROM-level table (filename, expected CRC, actual CRC, match/mismatch status)
  - Scan results: no ROM detail available (scan only knows filenames, not CRCs)

## Tests
- 17 API tests passed
- 21 NPS tests passed
- 14 version sort tests passed
- Frontend builds successfully

## Branch
- `agent/build-result-table`

## Commits
- `f00f63b` feat: add per-ROM details to build results, scan missing games table, ROM detail dialog
