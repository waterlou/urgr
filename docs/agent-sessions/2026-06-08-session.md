# Session: 2026-06-08

## Goal
Refactor NPS region handling from `year` to dedicated `region` field, fix game detail bugs (ROMs disappearing after rescrape, variant self-reference), and improve parent/group display.

## Files Changed

### Database schema
- **`libs/rom-manager/src/db/schema.rs`**: Added `region TEXT` column, changed UNIQUE to `(version_id, name, region)`
- **`apps/rom-manager-ui/server/db.js`**: Added `region` to JS schema + migration to recreate table

### Rust models & queries
- **`libs/rom-manager/src/models.rs`**: Added `region: Option<String>` to `GameEntry`
- **`libs/rom-manager/src/db/mod.rs`**: Updated `insert_game()`, `insert_games_batch()`, `list_games()` to include `region`
- All DAT parsers (`logiqx.rs`, `mame.rs`, `clrmamepro.rs`, `offlinelist.rs`) + tests: added `region: None`

### NPS CLI
- **`apps/nps-cli/src/main.rs`**: Fixed `extract_title_id()` to handle full NPS PKG filenames (extracts title_id from content_id pattern)

### Backend
- **`apps/rom-manager-ui/server/nps.js`**: Store region in `region` column, use original TSV name for clones (no suffix), store exact PKG filename from URL, store Content ID
- **`apps/rom-manager-ui/server/routes/collections.js`**: Include `region` in SELECT + GROUP BY, add `clone_regions` subquery for parent grouping, parse `regions` array in response
- **`apps/rom-manager-ui/server/routes/games.js`**: Include `region` in clones query, add `parent` info for variants, exclude self from variants, exclude parent from variant list when viewing a clone
- **`apps/rom-manager-ui/server/routes/games.js` (scrape)**: Include `roms` + `scanned_games` in scrape response

### Frontend
- **`src/components/GameListItem.jsx`**: Use `game.regions` array instead of `game.region`
- **`src/components/GameGridCard.jsx`**: Same
- **`src/components/GameDetail.jsx`**: Region badges from `game.region` + `game.year`, clickable parent badge for variants, region badge in variant items

## Commands Run
- `cargo build -p rom-manager`
- `cargo build -p parse-cli --release`
- `npx vite build`
- `bash server/test-api.sh` (14/14 passed)
- NPS import/clean/re-import (multiple times)

## Key Changes
- **Region in its own column**: `region` instead of abusing `year`
- **No clone name suffix**: Same name, different region → no `(EU)` suffix
- **Exact PKG filenames**: Uses the actual filename from the TSV URL
- **Scrape preserves ROMs**: `scrapeGameMetadata()` now includes `roms` and `scanned_games`
- **Variants exclude self**: `WHERE id != ? AND cloneof IS NOT NULL` for clones
- **Parent info on variant**: Clickable badge showing parent name + region
- **Multi-region display**: `regions: ['US', 'EU']` array on parent items
- **Content ID stored**: Used for Sony Store screenshot scraping
- **extract_title_id fix**: Handles full NPS PKG filenames like `UP4395-PCSE00890_00-...pkg`
