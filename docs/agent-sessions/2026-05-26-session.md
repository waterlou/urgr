# Session 2026-05-26

## Goal
Implement MAME name expansion and batch scraping with overwrite control.

## Changes
- `server/index.js`: Added MAME name expansion (DB lookup by short ROM name for arcade games, uses `unshift` as highest-priority search candidate). Extracted `scrapeSingleGame()` helper. Added `POST /api/games/batch-scrape` endpoint with SSE job (max 100 IDs, `overwrite` flag).
- `src/api.js`: Added `batchScrapeGameMetadata()` function.
- `src/components/GameBrowser.jsx`: Added batch scrape UI — "Scrape" button in toolbar, inline form with game count + overwrite toggle, SSE progress during scraping, summary results on completion.

## Commands
- `npm run build` (built successfully)
- Server restart
- Tested batch-scrape endpoint (returns jobId, validation works)
- `bash test-api.sh` — 14/14 passed
- `npx playwright test` — 7/7 passed (1 pre-existing skip)
